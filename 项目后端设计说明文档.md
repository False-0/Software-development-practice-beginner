# 医学刷题软件项目 (DuckMed)

一个基于 FastAPI 和 SQLAlchemy 构建的刷题系统，支持用户注册登录、浏览题目、做题目、错题浏览、收藏和历史记录、评论探讨等功能。

## 1-1 项目概述

这是一个仿医学刷题软件后端API服务，采用异步Python框架FastAPI开发，使用MySQL作为数据库存储，通过SQLAlchemy ORM进行数据访问。系统提供完整的用户管理、题目浏览、做题目、错题浏览、评论，收藏功能。

## 1-2 技术栈

- **后端框架**: FastAPI
- **数据库**: MySQL
- **ORM**: SQLAlchemy (异步)
- **数据库驱动**: aiomysql
- **密码加密**: passlib + bcrypt
- **缓存系统**: Redis
- **异步支持**: Python asyncio

## 1-3 项目结构

```
DuckMed/                          # 项目根目录
├── crud/                              # 数据访问层（CRUD操作）
│   ├── favorite.py                  # 收藏相关数据库操作
│   ├── errorhistory.py                   # 错题记录相关数据库操作
│   ├── questions.py                      # 题目相关数据库操作
│   └── users.py                     # 用户相关数据库操作
│   ├── correcthistory.py            #已斩题目记录相关数据库操作
|   ├── statistics.py                      #排行相关数据库操作
|   |—— comment.py                   #评论相关数据库操作
|   |—— post.py                #论坛相关数据库操作
|
├── models/                            # 数据模型定义
│   ├── favorite.py                  # 收藏数据模型
│   ├── errorhistory.py                   # 错题记录数据模型
│   ├── questions.py                      # 题目数据模型
│   └── users.py                     # 用户数据模型
│   ├── correcthistory.py            #已斩题目记录数据模型
|   ├── statistics.py                      #排行数据模型
|   |—— comment.py                   #评论数据模型
|   |—— post.py                #论坛数据模型
|
├── routers/                           # API路由定义
│   ├── favorite.py                  # 收藏相关API路由
│   ├── errorhistory.py                   # 错题历史记录相关API路由
│   ├── questions.py                      # 题目相关API路由
│   └── users.py                     # 用户相关API路由
│   ├── correcthistory.py            #已斩题目记录相关API路由
|   ├── statistics.py                      #排行相关API路由
|   |—— comment.py                   #评论相关API路由
|   |—— post.py                #论坛相关API路由
|
├── schemas/                         # 数据验证模型（Pydantic模型）
│   ├── favorite.py                  # 收藏数据验证模型
│   ├── errorhistory.py                   # 错题历史记录数据验证模型
│   ├── questions.py                      # 题目数据验证模型
│   └── users.py                     # 用户数据验证模型
│   ├── correcthistory.py            #已斩题目记录数据验证模型
|   ├── statistics.py                      #排行数据验证模型
|   |—— comment.py                   #评论数据验证模型
|   |—— post.py                #论坛数据验证模型
|
├── utils/                             # 工具函数目录
│
├── config/                             # 配置相关
│   ├── db_conf.py                    # 数据库配置文件
│   ├── cache_conf.py                 # Redis缓存配置
│ 
├── main.py                           # 应用入口文件
└── test_main.http                    # HTTP接口测试文件
```

## 1-4 功能模块

### 1. 用户管理模块

- 用户注册
- 用户登录
- 用户信息获取
- 用户信息更新
- 用户密码修改

### 2. 题目模块

- 题目分类获取
- 题目列表获取（支持分页和分类筛选）
- 题目详情获取
- 提交答案获取

### 3. 收藏模块

- 添加收藏
- 取消收藏
- 收藏列表获取
- 清空所有收藏
- 检查收藏状态

### 4. 浏览历史做对题目模块

- 添加浏览记录
- 浏览历史列表获取
- 删除单条浏览记录
- 清空浏览历史

### 5. 错题模块
- 获取错题总量
- 获取错的最多的章节
- 获取错题统计图
- 获取历史错题

### 6. 论坛模块
- 发表论点
- 查看论点详情
- 发表评论
- 删除评论


## 1-5 数据库设计

### 主要数据表

1. **用户表 (user)**
    - 用户基本信息存储
    - 包含用户名、密码(加密)、昵称、头像等字段

2. **用户令牌表 (user_token)**
    - 用户认证令牌管理
    - 支持令牌过期机制

3. **学科，章节分类表 
    - 学科，章节分类信息

4. **题目表 
    - 题目内容存储

5. **收藏表 (favorite)**
    - 用户收藏记录

6. **浏览历史表 
    - 题目历史记录

## 1-6 缓存设计

### 缓存策略

系统采用 Redis 作为缓存层，对高频访问的数据进行缓存，以提升系统性能和响应速度。

### 缓存数据类型



### 缓存更新机制

- 数据更新时自动清除相关缓存
- 采用缓存失效而非主动更新策略
- 支持批量清除特定模式的缓存

### 缓存配置

在 cache_conf.py 中配置 Redis 连接信息：

```python
REDIS_HOST = "localhost"
REDIS_PORT = 6379
REDIS_DB = 0
```


## 1-8 认证机制

系统使用基于令牌(Token)的认证机制：

1. 用户登录成功后返回访问令牌
2. 需要认证的接口在请求头中添加 `Authorization: token值`
3. 令牌有效期为7天

## 1-9 错误处理

系统提供统一的错误处理机制：

- 用户认证失败返回 401 状态码
- 资源不存在返回 404 状态码
- 服务器内部错误返回 500 状态码

## 1-10 开发规范

- 使用异步数据库操作
- 所有密码均加密存储
- 接口返回统一的 JSON 格式
- 详细的接口文档和示例
- 缓存操作封装成独立函数便于调用

## 1-11 性能优化

- 使用 Redis 缓存热点数据
- 异步数据库操作提升并发性能
- 合理的数据库索引设计
- 连接池管理减少连接开销

